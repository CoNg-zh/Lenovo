# === SECURITY: NEVER commit these ===
# Claude settings - contains API keys
.claude/
.claude.json

# Memory - personal context, daily logs (now included since repo is private)
# MEMORY.md excluded - may contain aggregated personal notes
MEMORY.md

# Environment files with secrets
.env
.env.*
*.key
*.pem
*.secret

# OS / editor
.DS_Store
*.swp
*.swo
*~

# Node
node_modules/

# OpenClaw internal state (now included)
.openclaw/workspace-state.lock
.openclaw/agents/

# Skills (reinstallable, large)
skills/
